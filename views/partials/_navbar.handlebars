<!--This file is a partial, indicated by the underscore at the beginning of the file title-->
<!--Bootstrap nav bar-->
<nav class="navbar navbar-expand-sm navbar-dark bg-dark mb-5">
  <div class="container">
    <a class="navbar-brand" href="/">Microbiology Data Portal</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/about">About</a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <!--If we are logged in, the global variable user will have a value, if not, show the login and register button-->
        {{#if user}}
          <li class="nav-item">
            <a class="nav-link" href="/vitek">Vitek Dataset</a>
          </li>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/users/logout">Logout</a>
          </li>
        {{else}}
          <li class="nav-item">
            <a href="/users/login" class="nav-link">Login</a>
          </li>
          <li class="nav-item">
            <a href="/users/register" class="nav-link">Register</a>
          </li>
        {{/if}}
        {{#if user.admin}}
          <li class="nav-item">
            <a href="/users/admin" class="nav-link"><i class="far fa-cog"></i> Admin</a>
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>